# my_xmrig

```
#!/bin/bash
sudo apt-get update
sudo apt-get install libhwloc-dev -y
sudo wget -P /root --no-check-certificate https://raw.githubusercontent.com/KANIKIG/my_xmrig/main/setup_miner.sh
sudo bash /root/setup_miner.sh
```

